# frontend/.env - Optional. Only create this if your API runs on a different URL.
# If you don't create .env, the app uses http://localhost:5000/api by default.

REACT_APP_API_URL=http://localhost:5000/api
