# backend/.env - Copy to .env and fill in values
PORT=5000
MONGODB_URI=mongodb://localhost:27017/wire-manufacturing
JWT_SECRET=your_very_long_super_secret_key_here_min_32_chars
JWT_EXPIRES_IN=7d
NODE_ENV=development
GROQ_API_KEY=your_groq_api_key_here
# Comma-separated frontend origins for production. Leave empty to allow all (dev).
# CORS_ORIGIN=https://your-frontend.example.com
# Optional seed passwords (used only by `npm run seed` on empty DB)
# SEED_ADMIN_PASSWORD=change-me-admin
# SEED_VIEWER_PASSWORD=change-me-viewer
CLOSING_PASSWORD=Ali786245
