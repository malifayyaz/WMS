require('dotenv').config({ path: __dirname + '/.env' }); require('mongoose').connect(process.env.MONGODB_URI).then(async () => { const RM = require('./models/RawMaterial'); const r = await RM.find(); console.log('RawMaterial', r.map(x=>({c: x.coilCategory, d: x.purchaseDate, w: x.weightInKg, isOB: x.isOpeningBalance}))); const RS = require('./models/ReadyStock'); const rs = await RS.find(); console.log('ReadyStock', rs.map(x=>({d: x.productionDate, w: x.weightKg, isOB: x.isOpeningBalance}))); process.exit(0); });
