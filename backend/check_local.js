require('mongoose').connect('mongodb://localhost:27017/wire-manufacturing').then(async () => { const RM = require('./models/RawMaterial'); const ob = await RM.find({isOpeningBalance: true}); console.log('OB:', ob.map(x=>x.weightInKg)); const sum = await RM.aggregate([{ \: { _id: null, total: { \: '\' } } }]); console.log('Total:', sum); process.exit(0); }).catch(e => { console.log('Local DB not running'); process.exit(0); });
